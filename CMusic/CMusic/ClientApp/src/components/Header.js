import React, { Component } from 'react';
import { NavMenu } from './NavMenu';
export class Header extends Component {
  render() {
    return (
      <header>
        <NavMenu />
      </header>
    );
  }
}
